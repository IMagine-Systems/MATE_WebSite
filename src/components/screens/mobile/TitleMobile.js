export default function  TitleMobile() {
    return (
        <section>
            <div style={{height:670, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center'}}>
                <span className="mobile_sub_title">MAKE</span>
                <span className="mobile_sub_title">A</span>
                <span className="mobile_sub_title">TEAM</span>
                <span className="mobile_sub_title">EXPERIENCE.</span>  
            </div>
            
        </section>
    );
}